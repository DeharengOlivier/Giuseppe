# Base de données PostgreSQL (Supabase)
# Connect to Supabase via connection pooling with Supavisor.
DATABASE_URL="postgresql://postgres.xxxx:[password]@aws-0-region.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection to the database. Used for migrations.
DIRECT_URL="postgresql://postgres.xxxx:[password]@aws-0-region.pooler.supabase.com:5432/postgres"

# Base de données MongoDB (analytics et tracking des visites)
# IMPORTANT: MongoDB doit être installé et en cours d'exécution
# Installation: voir MONGODB_SETUP.md
MONGODB_URI="mongodb://localhost:27017/portfolio_analytics"

# NextAuth Configuration
# URL de base de l'application
NEXTAUTH_URL="http://localhost:3000"

# CRITICAL SECURITY: Générez un secret unique et sécurisé pour la production!
# Générez-en un avec: openssl rand -base64 32
# NE JAMAIS utiliser la valeur par défaut en production!
NEXTAUTH_SECRET="CHANGE_THIS_IN_PRODUCTION_USE_OPENSSL_RAND_BASE64_32"

# Identifiants administrateur
# IMPORTANT: Changez ces valeurs AVANT le déploiement en production!
# Après modification, exécutez: npm run db:seed
ADMIN_EMAIL="admin@portfolio.com"
ADMIN_PASSWORD="Admin123!"

# Google OAuth
AUTH_GOOGLE_ID="votre-client-id-google"
AUTH_GOOGLE_SECRET="votre-client-secret-google"

# ⚠️  NOTES DE SÉCURITÉ IMPORTANTES ⚠️
#
# 1. NEXTAUTH_SECRET:
#    - Générez un secret cryptographiquement sécurisé
#    - Commande: openssl rand -base64 32
#    - Ne partagez JAMAIS ce secret publiquement
#    - Changez-le si compromis
#
# 2. ADMIN_PASSWORD:
#    - Utilisez un mot de passe fort (12+ caractères)
#    - Incluez majuscules, minuscules, chiffres, symboles
#    - Ne réutilisez pas de mots de passe existants
#    - Changez le mot de passe par défaut immédiatement
#
# 3. Production:
#    - N'utilisez JAMAIS les valeurs par défaut en production
#    - Stockez les secrets dans des variables d'environnement sécurisées
#    - Pour Vercel: utilisez les Environment Variables
#    - Pour autres hosts: utilisez leurs systèmes de gestion de secrets
#
# 4. MongoDB:
#    - En production, utilisez MongoDB Atlas ou un serveur sécurisé
#    - Activez l'authentification MongoDB
#    - Utilisez des connexions chiffrées (TLS/SSL)
#    - Format avec auth: mongodb://username:password@host:port/database
#
# 5. Fichier .env:
#    - Ne committez JAMAIS le fichier .env dans Git
#    - Le .gitignore doit inclure .env
#    - Partagez uniquement .env.example
#    - Chaque développeur crée son propre .env local
